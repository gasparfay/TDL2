//DEDE EL USUARIO
mientras(cliente.iniciarSesion(miEmail, miPsw) sea falso);

//DESDE CLIENTE
devolver (login(miEmail, miPsw, sesion));  //el metodo login busca la cuenta por email

//DESDE CUENTA
si(estaCuenta.getPsw() igual a miPsw)
    sesion.setAccount(estaCuenta)
    devolver verdadero;
sino 
    devolver falso ;
